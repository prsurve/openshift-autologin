{
  "name": "openshift-autologin",
  "version": "3.3.0",
  "description": "Chrome extension for auto-login to OpenShift and vSphere clusters",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint *.js",
    "lint:fix": "eslint *.js --fix",
    "validate": "npm run lint && npm run validate:manifest",
    "validate:manifest": "jq empty manifest.json && echo '✅ manifest.json is valid'",
    "package": "zip -r extension.zip manifest.json popup.html popup.js content.js background.js icon*.png README.md -x '*.git*' 'node_modules/*' 'dist/*' '.claude/*'",
    "hooks:install": "./.github/hooks/install.sh"
  },
  "keywords": [
    "chrome-extension",
    "openshift",
    "vsphere",
    "auto-login",
    "authentication"
  ],
  "author": "prsurve",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^17.8.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/prsurve/openshift-autologin.git"
  }
}
